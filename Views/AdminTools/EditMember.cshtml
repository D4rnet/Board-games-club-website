@model NewMember
@{
    ViewData["Title"] = "Редактирование участника";
}

<form method="post" class="card border-secondary mt-5">
    <input asp-for="@Model.member.id" hidden="" />
    <div class="card-header d-flex justify-content-between align-items-center">
        <p class="m-0">Редактирование участника</p>
        <a class="btn btn-outline-secondary" asp-controller="AdminTools" asp-action="MembersTable"><i class="bi bi-box-arrow-left"></i></a>
    </div>

    <div class="card-body">
        <div class="form-group">
            <div class="mt-2">
                <label asp-for="@Model.member.userEmail" class="form-label"></label>
                <select asp-for="@Model.member.userEmail" class="form-select" disabled="">
                    @{
                        foreach (var obj in Model.users)
                        {
                            <option>@obj.email</option>
                        }
                    }
                </select>
                <input asp-for="@Model.member.userEmail" hidden="" />
                <span class="text-danger" style="font-size: 14px;" asp-validation-for="@Model.member.userEmail"></span>
            </div>

            <div class="mt-2">
                <label asp-for="@Model.member.rentId" class="form-label"></label>
                <select asp-for="@Model.member.rentId" class="form-select">
                    @{
                        foreach (var obj in Model.openRents)
                        {
                            <option>@obj.rentId</option>
                        }
                    }
                </select>
                <span class="text-danger" style="font-size: 14px;" asp-validation-for="@Model.member.rentId"></span>
            </div>

            <div class="mt-2 mb-4">
                <label asp-for="@Model.member.peopleCount" class="form-label"></label>
                <input asp-for="@Model.member.peopleCount" type="number" class="form-control" placeholder="Введите сколько вас будет">
                <span class="text-danger" style="font-size: 14px;" asp-validation-for="@Model.member.peopleCount"></span>
            </div>

            <div class="modal-footer mt-2">
                <button type="submit" class="btn btn-primary">Обновить</button>
            </div>
        </div>
    </div>
</form>